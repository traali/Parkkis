pre-commit:
  parallel: false
  commands:
    biome-lint:
      glob: "*.{js,ts,jsx,tsx,json}"
      run: npx @biomejs/biome lint {staged_files}
pre-push:
  parallel: false
  commands:
    monastery-visit:
      run: npm run visit
