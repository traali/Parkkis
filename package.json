{
  "name": "parkkis",
  "version": "2.6.0",
  "description": "Parking in Helsinki is a cognitive minefield. Complex zone rules, resident-only restrictions, and \"trap\" spots with confusing signage lead to avoidable fines. Drivers need more than just \"free spots\"—they need to know *safe* spots.",
  "main": "index.js",
  "scripts": {
    "fetch": "cd engine && npx ts-node src/fetch.ts && npx ts-node src/fetch_signs.ts && npx ts-node src/fetch_roadworks.ts && npx ts-node src/fetch_liipi.ts",
    "process": "cd engine && node src/process.js",
    "ingest": "npm run fetch && npm run process",
    "dev": "cd web && npm run dev",
    "build": "cd web && npm run build",
    "lint": "npx @biomejs/biome lint .",
    "check": "npx @biomejs/biome check --write ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@turf/turf": "^7.3.4",
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^25.2.2",
    "axios": "^1.15.2",
    "dotenv": "^17.2.4",
    "fastify": "^5.8.5",
    "fs-extra": "^11.3.3",
    "p-limit": "^3.1.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.14",
    "vite": "^8.0.10"
  }
}
